services:
  - type: web
    name: render-workflow-docs
    runtime: static
    plan: free
    buildCommand: echo "No build needed"
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
